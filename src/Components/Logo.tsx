import React from 'react';
import logo from '../assets/images/bestbuyproperties-default-logo.png';

const Logo = (props: any) => {
    return <img alt={'Bestbuy properties auctions logo'} {...props} src={logo} height={'64px'} />;
};

export default Logo;
